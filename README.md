# lightning-nodejs-example

Basic example code to interface with an LND lightning node. 

Run `npm i` to install the only dependency (`axios`) and update the `run()` function to enable other features of the script. 
